A simple set of .java files which simulate multiple Client interactions with one another via a single server. The server is host on localhost and implements a basic client to client chat capablilities.
